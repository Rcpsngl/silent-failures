# Last Session

<!--
  This is the unique append anchor. A new session block goes immediately BELOW
  this line.

  Do not write by matching the heading patterns themselves — they are documented
  in ../README.md, deliberately, and not here. A block once got wedged into a
  file's own format documentation and the parser silently served the wrong day.

  Each marker must appear exactly once in this file, at the start of its line.
  The validator enforces both.
-->

## Session: 2026-01-01 — short title

What was discussed, what got done, what was left half-finished. Written for the
next session, not for a reader who already knows: name files, name decisions,
say what was ruled out and why.

Anything the next session should do FIRST goes at the top of this block.

## Previous Sessions

A few lines each, oldest trimmed away over time. This section is not injected
into context; it exists so the file does not become a graveyard.
